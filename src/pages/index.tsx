import { FC, useState } from "react";

import SideBar from "@/components/sidebar/SideBar";

const Home: FC = () => {
    return (
        <div>
            <SideBar />
        </div>
    );
};

export default Home;
